# Use H2 in PostgreSQL-compatibility mode
db.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
db.user=sa
db.password=

# Flyway migrations (so you can run migrations in tests if desired)
flyway.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
flyway.user=sa
flyway.password=
